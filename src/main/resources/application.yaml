spring:
  datasource:
    url: jdbc:postgresql://35.194.212.4:5432/postgresql
    username: postgresql
    password: postgrespassword
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
server:
  port: 8080